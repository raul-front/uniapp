const state = {
  isiPhoneX: false,
}

const getters = {
}

const actions = {
}

const mutations = {
  setIPhoneX (state) {
    state.isiPhoneX = true
  },
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
